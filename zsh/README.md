# installation
```
ln -s ~/.config/zsh/.zshrc ~/.zshrc
ln -s ~/.config/zsh/.zshrc-personal ~/.zshrc-personal
```

- or using ~/.zshenv to change the defualt environment
- in `~/.zshenv`:
```
ZDOTDIR=~/.config/.zshenv
```
